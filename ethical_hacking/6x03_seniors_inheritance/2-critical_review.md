# Source Material Critical Review

| Finding | First impression | Zone of doubt or confidence | To investigate in Task 3 |
| --- | --- | --- | --- |
| F-01 | Sarah identifies a clear cross-tenant authorization gap in the results API. | High confidence on first read: the evidence directly shows that one laboratory's authenticated user could retrieve another laboratory's result by substituting a result identifier. | Confirm tenant-isolation enforcement at the data-access layer, assess how many result fields are exposed, and evaluate the impact of health-data disclosure under the HDS and RGPD context. |
| F-02 | Sarah sees a stored-content issue in the support portal that is technically valid but limited in impact by the supported analyst flow. | Moderate confidence: the execution proof is strong, but the actual business impact depends on support-user privileges, browser controls, and whether the support portal is a privileged cross-tenant surface. | Verify the support portal trust model, confirm whether the affected rendered content is read by multiple laboratory tenants, and assess the extent to which the portal's managed workstation controls reduce exposure. |
| F-03 | Sarah identifies a token-scoping problem in the hospital integration gateway that could create cross-tenant access over a connector boundary. | High confidence on first read: the broad `integration:all` scope and cross-tenant acceptance are a legitimate design weakness even before environmental calibration. | Confirm whether the private integration network and gateway restrictions materially reduce live exploitability, verify whether token audience and tenant claims are enforced, and assess the operational impact of other connector roles using the same trust boundary. |
| F-04 | Sarah identifies a configuration weakness in a legacy connector worker, but the practical severity is uncertain because the app routes through a managed gateway. | Material uncertainty: the gateway may materially reduce the live risk, so this issue should be treated as a defense-in-depth gap unless the private network path and bypass conditions are validated. | Confirm the actual connector traffic path, verify whether the managed mTLS gateway enforces certificate validation and destination isolation, and determine whether the worker-level flag could be abused when the gateway control fails or is misconfigured. |
| F-05 | Sarah identifies a message-integrity weakness in the sample-status webhook that could permit false state transitions without a cryptographic signature. | Moderate confidence: the issue is real on its face, but the operational impact depends on whether downstream workflows trust this status field for result release, lab operations, or export controls. | Measure how sample-state changes affect release logic, determine whether the partner allowlist is the primary control, and inspect state-transition enforcement and replay protections in production. |
| F-06 | Sarah sees a retention mismatch between the documented expiry period and the actual validity period of export links. | Moderate confidence: the evidence is strong for a retention-control deficiency, but the wider impact depends on DPO-approved retention policy, object-access logging, and the volume of exports involved. | Validate the approved retention policy, verify whether the retention mismatch is limited to test artifacts or live patient exports, and assess HDS and RGPD accountability implications for health-data retention. |
| F-07 | Sarah identifies a bulk-export capability that appears materially broader than a typical Support Manager role should have, but production controls may already constrain it. | High uncertainty requiring validation: the workflow may be an intended DPO-controlled emergency process rather than an unmitigated privilege issue, so the finding should not be accepted without confirming enforcement of approvals and usage patterns. | Confirm whether every production path requires a DPO ticket, WebAuthn step-up, dual approval, case-specific encryption, and seven-day expiry, and verify whether the observed test role is representative of live operating conditions. |
| F-08 | Sarah identifies a password-recovery enumeration issue that reveals whether an account exists and may support targeted identity attacks. | Moderate confidence: the issue is valid as a generic account-enumeration weakness, but the actual impact depends on the prevalence of SSO, MFA coverage, and the value of laboratory-user identities in the target environment. | Assess the proportion of local accounts versus SSO-backed accounts, confirm MFA coverage, and evaluate whether the observed behavior is exploitable in a support or account-takeover workflow relevant to laboratory operations. |
| F-09 | Sarah identifies a stale-authorization problem where an administrator keeps write privileges after a role downgrade until the cache refreshes. | High confidence on first read: the observed stale session behavior is a credible authorization-lifecycle weakness that can affect real privilege changes. | Confirm whether this shares the same root cause as F-03, evaluate whether role downgrades are common in operations, and determine whether same-session persistence is mitigated by direct session revocation and cache invalidation. |
| F-10 | Sarah identifies a service-to-service trust issue in which the analytics service accepts a caller-supplied laboratory header without independent verification. | High confidence on first read: the proof shows the service directly trusts a tenant header that should have been derived from the gateway or service identity. | Verify which service principals can reach the analytics service, determine whether the same service trust boundary is reachable through broader connector tokens, and assess whether this header trust creates an easier route to cross-laboratory data exposure. |
